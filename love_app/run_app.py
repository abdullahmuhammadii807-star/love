import os`nimport sys`nimport time`nimport webbrowser`nimport streamlit.web.cli as stcli`n`nif __name__ == "__main__":`n    if getattr(sys, "frozen", False):`n        base_dir = sys._MEIPASS`n    else:`n        base_dir = os.path.dirname(os.path.abspath(__file__))`n    script_path = os.path.join(base_dir, "love2.py")`n    def open_browser():`n        time.sleep(2)`n        webbrowser.open("http://localhost:8501")`n    import threading`n    threading.Thread(target=open_browser, daemon=True).start()`n    sys.argv = ["streamlit", "run", script_path, "--global.developmentMode=false", "--server.headless=true"]`n    sys.exit(stcli.main())
